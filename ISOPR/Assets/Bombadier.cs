using UnityEngine;
using System.Collections;

public class Bombadier : MonoBehaviour {

    //health 100
    //lay rate 1 bomb over 10 seconds then flys away
    //speed medium
    //Starts spawning at tower level 5
    //spawn rate, 1 + 1 every 2 levels

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
